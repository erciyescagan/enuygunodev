{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_post_index' %}

{% block main %}

    <h4>Project {{ projectname }} can be completed in {{ projectweek }} weeks and {{ projectday }} days with these assignments.</h4>

    <h3>Assignments</h3>

    <table class="table table-striped table-middle-aligned">
        <thead>
            <tr>
                <th scope="col"><i class="fa fa-project-diagram" aria-hidden="true"></i> Project</th>
                <th scope="col"><i class="fa fa-male" aria-hidden="true"></i> Developer</th>
                <th scope="col"><i class="fa fa-tasks" aria-hidden="true"></i> Task</th>
                <th scope="col"><i class="fa fa-calendar" aria-hidden="true"></i> Time</th>
                <th scope="col" class="text-center"><i class="fa fa-cogs" aria-hidden="true"></i>Difficulty</th>
            </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr>
                <td>{{ projectname }}</td>
                <td>{{ post.dname }}</td>
                <td>{{ post.task }}</td>
                <td  class="text-right">{{ post.ttime }}</td>
                <td  class="text-right">{{ post.tdifc }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4" align="center">No Assignments found</td>
           </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block sidebar %}
{#    <div class="section actions">#}
{#        <a href="{{ path('admin_post_new') }}" class="btn btn-lg btn-block btn-success">#}
{#            <i class="fa fa-plus" aria-hidden="true"></i> {{ 'action.create_post'|trans }}#}
{#        </a>#}
{#    </div>#}

    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %}
